apiVersion: v1
kind: ServiceAccount
metadata:
  name: driftguard
  namespace: driftguard
  labels:
    app.kubernetes.io/name: driftguard
  # Annotate with the IRSA role ARN from `terraform output app_irsa_role_arn`:
  # annotations:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::<account>:role/driftguard-app
