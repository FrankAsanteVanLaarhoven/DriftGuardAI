apiVersion: v1
kind: Service
metadata:
  name: driftguard
  namespace: driftguard
  labels:
    app.kubernetes.io/name: driftguard
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: driftguard
  ports:
    - name: http
      port: 8000
      targetPort: http
