# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Environment / secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
aws_credentials

# MLflow / DVC local state
mlflow.db
mlruns/
.dvc/cache/
.dvc/tmp/

# Data + large artifacts (DVC-tracked; not committed directly).
# The committed exceptions below are the always-loadable fallback + metrics.
data/*.parquet
artifacts/primary.joblib
artifacts/*.tmp
models/primary_pointer
artifacts/evidently.html

# Node / editor
node_modules/
.DS_Store
.idea/
.vscode/
.dvcstore/
